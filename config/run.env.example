# Optional runtime overrides for `npm run ghost-audit:parallel`
#
# Usage:
# 1) Copy to `config/run.env` (this file is gitignored)
# 2) Run: `npm run ghost-audit:parallel`

# Base RPC (archive-capable)
FORK_RPC_URL=https://base-mainnet.public.blastapi.io

# Target realized harvest (block + tx hash)
REALIZED_BLOCK=41493767
REALIZED_TX_HASH=0x1f19267099524175eb02901712c87f07185cd4f422e45f592b469b02b9b33122

# Window selection: set one of these
# Option A: compute `BASE_START_BLOCK` from timestamp delta
WINDOW_SECONDS=21600
# Option B: hardcode a start block (disables WINDOW_SECONDS compute)
# BASE_START_BLOCK=41493000

# Simulation grid:
# - With OFFSETS_CSV=0,20, use STEP=40 to cover every 20 blocks.
OFFSETS_CSV=0,20
STEP=40

# Anvil/worker logs
LOG_DIR=/tmp/ghost-audit

# Optional: gas limit for harvest txs (skips eth_estimateGas)
# GAS_LIMIT=3000000

